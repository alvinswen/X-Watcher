# X-Watcher Web 后台操作手册

> 面向运营与产品使用者。采集自代码快照 `a3c67f7`（v1.51.2）· 2026-08-06
> 阅读前请先了解一件事：Web 后台是**查看与管理**入口，不承载智能环节。摘要、翻译、议题分类、综述撰写都由 AI Agent 通过 MCP 完成，后台看到的是它们的产出。

---

## 一、开始之前

### 1.1 访问地址

服务默认监听 `http://127.0.0.1:8000`（**仅本机**）。若需从局域网其他机器访问，需在启动时显式指定 `--host 0.0.0.0`——这是 v1.38.0 出于安全考虑收窄的默认值。

### 1.2 必做第一步：配置 API Key

**所有管理类页面在未配置 API Key 时都是空白占位状态**，并会自动弹出一次引导框。

配置方式：点击侧边栏底部的 API Key 状态指示灯 → 粘贴管理员 API Key → 保存。

Key 保存在浏览器本地（`localStorage`），换浏览器或清缓存后需重新配置。若 Key 失效（服务端返回 401），指示灯会变为失效状态，并弹出带"重新配置"按钮的提示。

> API Key 从哪来：由管理员在「用户管理」页创建用户时一次性发放，或由 `x-watcher init` 初始化时生成。**创建后只显示一次**，务必当场复制保存。

### 1.3 界面结构

左侧固定侧边栏（可折叠为窄条），包含 9 项菜单；底部是三个全局控件：API Key 状态灯、主题切换（亮色 → 暗色 → 跟随系统，三态循环）、侧边栏折叠。

顶部 header 常驻 **Twitter 余额指示器**，按配置的阈值变色告警。

---

## 二、页面速查

| 菜单 | 路径 | 用途 |
|---|---|---|
| 仪表盘 | `/dashboard` | 6 个统计卡 + 配置校验 + 抓取任务表 |
| 推文浏览 | `/browse` | **日常主力阅读页** |
| 推文搜索 | `/search` | 关键词 + 作者 + 日期检索 |
| 推文管理 | `/tweets` | 推文列表与详情 |
| 关注管理 | `/follows` | 抓取账号的增删改与档案 |
| 信源评审 | `/candidates` | 候选信源的批准/否决 |
| 议题管理 | `/subjects` | 议题 CRUD + 事件流/摘要/综述 |
| 用户管理 | `/users` | 用户与凭据 |
| 数据同步 | `/sync` | 导出与导入 |

> 命名提示：侧边栏叫「关注管理」，进去后页面标题是「抓取账号管理」——指的是同一件事。

---

## 三、日常阅读：推文浏览

`/browse` 是使用频率最高的页面。

**选内容**：左侧日历面板按日期选择（每个日期显示当天推文数），作者面板可进一步筛选到单个账号。也可以切换到「作者时间线」模式，按账号连续查看。

**过滤噪音**：打开「长推文过滤」开关，只看正文长度超过阈值的推文（默认 280 字符，可调）。这个开关的状态跨页面保持。

**沉浸阅读**：点全屏按钮进入沉浸模式，用快捷键退出。

**分享**：单条推文可一键复制为 Markdown 格式，方便贴到其他地方。

每条推文卡片会同时显示原文与中文摘要/翻译（如果 Agent 已经处理过）。**没有摘要说明还没轮到它被处理**——后台无法手动触发翻译，这是 Agent 侧的工作。

---

## 四、抓取管理

### 4.1 管理抓取账号（`/follows`）

表格列出：用户名（带头像，点击展开档案抽屉）、平台永久 ID、添加理由、极简介绍、添加人、添加时间、状态。

> 「平台永久 ID」缺失时显示黄色「待补全」标记。系统内部以这个不变 ID 关联数据，而非用户名——用户改名不会导致历史数据断链。

**添加账号**需要填：

| 字段 | 要求 |
|---|---|
| 用户名 | 不含 `@` |
| 添加理由 | **至少 5 个字符** |
| 极简介绍 | **不超过 10 个汉字**，例如「加密货币交易所」 |

**行操作**：查看档案 / 编辑 / 禁用⇄启用。禁用不删除历史数据，只是不再抓取。

**工具栏**三个按钮：

- **立即抓取** — 触发一次抓取任务（需已配置 API Key 才可见）
- **同步档案** — 拉取各账号在 X 上的最新资料（头像、简介、粉丝数）
- **添加账号**

### 4.2 查看抓取进度（`/dashboard`）

仪表盘下方的任务表列出抓取任务的 ID、状态、创建时间。

> 后台**没有**取消或重试任务的入口，也没有抓取调度配置界面。任务状态是内存态，**服务重启后任务历史会清空**。

仪表盘上方 6 个统计卡：推文总数、活跃关注、摘要总数、今日新增、待摘要推文、数据占用。

「配置校验」卡片显示当前配置是否合法，可手动刷新。

### 4.3 余额监控

header 的 Twitter 余额指示器实时显示 TwitterAPI.io 剩余额度，按 `.env` 里配置的两档阈值变色（默认警告 50000、危险 10000，按 15 credits/条计费，实测日均约 4050 credits）。

---

## 五、议题管理（`/subjects`）

议题是长期订阅的主题。**它的匹配不是关键词检索，而是由 Agent 读你写的自然语言描述后自行判定**——所以描述写得越清楚，分类越准。

### 5.1 创建与维护

左侧列表可按状态筛选（全部 / 活跃 / 暂停）。

**活跃议题上限 20 个**，达到上限后「新建议题」按钮会置灰，需要先暂停或删除一些。

新建时填：议题名、语义描述（关键，Agent 靠它判定）、关键词标签（回车添加，仅作展示）、状态。

### 5.2 三个 Tab

- **事件流** — 命中该议题的推文，顶部提示上次分类时间
- **摘要** — 按时间区间生成的滚动新闻（L1），回答"这段时间发生了什么"
- **综述** — 累积的活文档（L2），回答"到目前为止我们知道什么"

综述 Tab 功能最丰富：正文按 section 折叠；**引证可展开回溯到原推文**（原推文已不可查时显示提示）；可手动请求综述更新；可查看历史版本并在版本间切换、回到最新。

> 「请求综述更新」只是挂一个待办标记，实际撰写由 Agent 完成，不会立即出结果。

---

## 六、信源评审（`/candidates`）

系统会从已抓取的推文中，自动发现被频繁引用但尚未在关注列表里的账号，形成候选队列等你决策。

**状态筛选**六档：待审 / 全部 / 已发现 / 已预审 / 已批准 / 已否决。

典型流程：Agent 先做三维预审打分（原创性 / 差异度 / 专业度，各 0–10 分，每维附样本内证据推文），你在候选档案卡上看到评分与证据后做终审。

**批准** → 可填极简介绍（≤10 汉字）→ 批准后会自动加入抓取名单，提示里带跳转「关注管理」的链接。

**否决** → 可填否决原因（供后续复盘）→ 该账号将被永久抑制，不再出现在候选队列。

档案卡会标记风险信号，如「机构认证」「自动化账号」黄标。

> 若两人同时评审同一候选，后提交的会收到冲突提示并提供刷新链接。

---

## 七、用户与凭据（`/users`）

创建用户后会**一次性展示临时密码与 API Key**，都带复制按钮。**这个界面关闭后无法再次查看**，必须当场保存并转交给对应的人。

可编辑用户名称、邮箱、管理员开关，也可重置密码（同样一次性展示）。

> 安全提示：连续 5 次登录失败会触发限流，锁定 15 分钟。注意这是**全实例**限流——锁定期内所有人都无法登录，包括密码正确的。服务重启可立即解除。

---

## 八、数据导出与导入（`/sync`）

### 8.1 导出

选择数据分类（配置数据 / 内容数据，可多选），可选填时间范围、作者过滤（逗号分隔）、实例标识（默认 `web-export`）。导出后显示统计信息。

### 8.2 导入

上传导出文件 → 查看文件信息 → 选择要导入的分类 → 选择**冲突策略** → 预览 → 执行。

三种冲突策略：

| 策略 | 行为 |
|---|---|
| 跳过（skip） | 已存在的记录不覆盖 |
| 覆盖（overwrite） | 已存在的记录被替换 |
| 合并（merge） | 与已有数据合并 |

**务必先看预览再执行**——预览是 dry-run，在临时副本上模拟，不会改动真实数据。

> ⚠️ 重要：导入操作与 Agent 的摘要回写**不能同时进行**（存储层只有进程内锁）。执行导入前请确认没有 Agent 任务在跑。

---

## 九、常见问题

**推文没有中文摘要？**
摘要由 Agent 生成，后台无法触发。检查是否有 Agent 任务在跑，或联系负责该环节的同事。

**点了「立即抓取」没反应？**
去仪表盘看任务表状态。若已有任务在运行中，系统会拒绝新建——同一时刻只允许一个抓取任务。

**页面一片空白，什么都没有？**
八成是没配 API Key，或 Key 已失效。看侧边栏底部的状态灯。

**议题一直没有新命中？**
议题分类由 Agent 异步完成。事件流顶部会显示"上次分类时间"，如果时间很久远，说明 Agent 侧没有在跑分类任务。

**请求转圈很久后报超时？**
前端请求超时是 30 秒。大数据量的导出可能超时，建议缩小时间范围分批导出。

**改了账号的 `manual_limit` 但抓取量没变？**
手动上限优先级最高但只在下次抓取生效，且实际抓取量还受页数上限约束（默认每账号每次最多 10 页、每页 20 条）。

**字体显示不正常？**
界面使用的霞鹜文楷等字体从公网 CDN 加载，内网或断网环境会回退到系统字体，不影响功能。

---

## 十、界面之外

有些能力**只在 MCP 侧提供**，后台没有入口：

| 能力 | 途径 |
|---|---|
| 生成摘要与翻译 | Agent 调 `get_unsummarized_tweets` + `save_summaries` |
| 议题分类 | Agent 调 `put_subject_matches` |
| 撰写滚动新闻 / 综述 | Agent 调 `put_subject_digest` / `put_subject_review` |
| 挖掘信源候选 | Agent 调 `mine_source_candidates` |
| 回溯历史推文（backfill） | Agent 调 `trigger_backfill` |
| 查审计日志 | 需在服务器日志文件中检索 |

详见《MCP 工具参考手册》。
